[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run stablecoin_dashboard.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
